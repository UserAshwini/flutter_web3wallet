import 'package:flutter/material.dart';

abstract class Palette {
  static const heading = Color.fromARGB(255, 106, 106, 106);
  static const textPrimary = Colors.deepPurple;
  static const textSecondary = Colors.black;
}